This project does aim to query the  "https://macaddress.io/ macaddress lookup API and fetch the company details and macadress.

To run these Python project we have to run main.py file in command line.
